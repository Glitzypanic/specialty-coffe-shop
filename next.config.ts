import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Use default Next.js behavior. Keep this file if you need to opt-in to
  // customizations later. For a minimal project this can be empty.
};

export default nextConfig;
